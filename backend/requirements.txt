# Core Web Framework
fastapi
uvicorn
python-multipart
starlette

# Environment & Config
python-dotenv
pydantic
pydantic-settings

# LangChain Core (let pip resolve compatible versions)
langchain-core>=0.3.43
langchain-google-genai
langgraph

# API Client
httpx
requests

# Async
anyio

# Data Processing (minimal)
pandas
numpy

# That's it! Removed heavy dependencies:
# - sentence-transformers (too large)
# - chromadb (not needed)
# - pinecone (disabled)
# - langchain-community (too large)
# - torch/transformers (too large)